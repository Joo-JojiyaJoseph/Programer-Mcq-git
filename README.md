# PSC Assistant Programmer 2026 — MCQ React App

Simple React + Vite app replicating the PSC Assistant MCQ UI for local use and GitHub hosting.

Quick start

1. Install dependencies:

```bash
npm install
```

2. Run dev server:

```bash
npm run dev
```

3. Build for production:

```bash
npm run build
npm run preview
```

Deploy to GitHub Pages

1. Build: `npm run build`
2. Push `dist` to `gh-pages` branch or use GitHub Actions to deploy `dist` to Pages.
